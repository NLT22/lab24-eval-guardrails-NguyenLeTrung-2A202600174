# =====================================================================
# Lab 24 - Full Evaluation & Guardrail System
# Python 3.10+ recommended. This file is compatible with the copied Lab 18 venv.
#
# Install:
#   python -m pip install -r requirements.txt
#
# Note:
# - guardrails-ai is NOT listed because this environment/package index returns
#   "No matching distribution found for guardrails-ai".
# - Lab 24 topic guard is implemented locally in phase-c/topic_guard.py.
# =====================================================================

# ---- Config & secrets --------------------------------------------------
python-dotenv==1.2.2
requests>=2.32.0
PyYAML>=6.0.2

# ---- Data handling, analysis, reports ---------------------------------
pandas>=2.2.0
numpy==2.2.6; python_version < "3.11"
numpy>=2.2.6; python_version >= "3.11"
scikit-learn>=1.5.0

# ---- OpenAI-compatible clients / RAGAS --------------------------------
openai>=1.0.0,<3.0.0
ragas>=0.2.0,<0.3.0
datasets>=2.18.0,<4.0.0

# ---- LangChain packages used by RAGAS synthetic/eval workflows ---------
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
tiktoken>=0.8.0

# ---- RAG retrieval pipeline from Day 18 -------------------------------
sentence-transformers==5.4.0
maturin>=1.0.0
qdrant-client==1.13.3
rank-bm25==0.2.2
underthesea>=6.8.0

# ---- Reranking ---------------------------------------------------------
FlagEmbedding==1.3.5
flashrank==0.2.10

# ---- Guardrails / PII / safety ----------------------------------------
presidio-analyzer>=2.2.355
presidio-anonymizer>=2.2.355
phonenumbers>=8.13.0
spacy>=3.7.0

# guardrails-ai intentionally omitted:
#   pip could not find compatible distributions in this environment.
#   The repo uses phase-c/topic_guard.py instead of Guardrails AI ValidTopic.

# ---- HuggingFace / Llama Guard / local models --------------------------
huggingface-hub>=0.27.0
accelerate>=1.2.0
transformers>=4.45.0
safetensors>=0.4.5
torch>=2.3.0

# ---- PDF pre-processing ------------------------------------------------
pymupdf==1.27.2.3
pymupdf4llm==1.27.2.3

# ---- Optional reporting/demo ------------------------------------------
matplotlib>=3.9.0
seaborn>=0.13.0
streamlit>=1.40.0
gradio>=5.0.0

# ---- Dev / validation --------------------------------------------------
pytest==9.0.3
ruff==0.15.12
yamllint>=1.35.0
